<li>
  <%= link_to(tasks_path(category_id: category.id), class: "flex items-center p-2 text-base font-normal text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700") do %>
    <span class="ml-3"><%= category.name %></span>
  <% end %>
  </a>
</li>
